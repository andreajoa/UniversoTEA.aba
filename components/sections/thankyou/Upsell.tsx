export default function Upsell() {
  // Upsell removed from the thank-you page per request.
  // Keeping this placeholder component prevents import errors
  // while allowing quick re-enable in the future.
  return null;
}
